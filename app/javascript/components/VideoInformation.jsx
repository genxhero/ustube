import React from 'react'
import PropTypes from 'prop-types'

const VideoInformation = (props) => {
    const {currentUser, creator, information} = props
    return (
        <div>
            
        </div>
    )
}

export default VideoInformation;