import React from 'react';

/**
 * Options for a user.
 */

const UserOptions = props => {
    const user = { props }
    return (
        <div className="user-options">
            user options
    </div>)
}

export default UserOptions;